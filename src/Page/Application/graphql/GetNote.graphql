#import "./NoteFragment.graphql"

query SearchNotes (
  $text: String!,
){
  notes(
    find: {
      where: {
        text: {
          contains: $text
        }
      }
    }
  ) {
    ...NewNote
  }
}
