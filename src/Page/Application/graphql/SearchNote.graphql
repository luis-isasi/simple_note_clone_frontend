query SearchNote (
  $text: String!,
){
  notes(
    find: {
      where: {
        text: {
          contains: $text
        }
      }
    }
  ) {
    id
    text
    user {
      id
      email
    }
    tags {
      name
    }
    }
}
